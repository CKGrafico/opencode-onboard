## Basic Memory

- Use basic-memory MCP tools (`search`, `write_note`, `edit_note`, `build_context`, `recent_activity`) for cross-session context: `search` for prior decisions before implementing unfamiliar areas, `write_note` for architecture decisions and cross-agent context.
- Do NOT run `basic-memory` in bash: it is an MCP server.
