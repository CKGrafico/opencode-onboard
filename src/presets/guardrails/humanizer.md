## Humanizer

- Load the `@humanizer` skill via the `skill` tool before writing prose (commit messages, PR descriptions, docs, proposals) to remove AI writing patterns and sound more natural.
- Do NOT apply humanizer to code, config files, or terminal output: only to prose.
